package com.qualityplus.automatizacao;

import static org.junit.Assert.assertEquals;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

import com.thoughtworks.selenium.Selenium;

public class Login {

/*	public static void  efetuarLogin(Selenium driver, String login,String senha){
	    driver.get("http://www.sbc.org.br/");
	    assertEquals("Sociedade Brasileira de Computação - SBC / Página Inicial", driver.getTitle());
	    driver.findElement(By.id("mod_login_username")).clear();
	    driver.findElement(By.id("mod_login_username")).sendKeys(login);
	    driver.findElement(By.id("mod_login_password")).clear();
	    driver.findElement(By.id("mod_login_password")).sendKeys(senha);
	    driver.findElement(By.xpath("//input[@name='Submit']")).click();
	}
	*/
}






